# Versión de AppVeyor
version: 2.0.{build}

# Imagen del entorno de construcción (Windows)
image: Visual Studio 2019

# Variables de entorno
environment:
  php_version: '8.1.12'

# Instalación de PHP y Composer
install:
  - ps: Install-Product php $env:php_version x64
  - ps: Install-Product composer

# Instalar dependencias de PHP usando Composer
before_build:
  - composer install

# Construcción y pruebas
build_script:
  - echo Build started
  - php -l src # Verifica que los archivos PHP no tienen errores de sintaxis

